plugins {
    id 'java-conventions'
    id 'java-library'
}

group = 'br.com.opinai.api.conta.application'

dependencies {
    implementation(project(":shared"))
    implementation(project(":api-conta:domain"))
}
